# Default values for auth-db-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: skydiverkhv/db_auth_service
  #  Значение	Что делает pullPolicy
  #  Always	всегда тянет образ
  #  IfNotPresent	только если нет локально
  #  Never	не тянет вообще
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: v1.1.13

service:
  type: ClusterIP
  port: 8084

db_properties:
  url: jdbc:postgresql://postgres-auth-db-service.database.svc.cluster.local:5432/hotel_auth_db_data?ssl=false
  ddl_mode: update

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 256Mi
secret:
  name: postgres-hotel-db-secret # ✅ переименовано для ясности
  username: postgres
  password: postgres
  db: hotel_db



